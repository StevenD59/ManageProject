{% extends 'admin/index.html.twig' %}

{% block body %}
    <div class="col-12 col-md-10 text-center">
        <h1 class="mb-5">{{ title }}</h1>

        <table class="table">
            <thead>
            <tr>
                <th class="col-4">Nom</th>
                <th class="col-4">Prénom</th>
                <th class="col-4">Actions</th>
            </tr>
            </thead>
            <tbody>
            <tr>
                {% for users in user %}
                <th scope="row">{{ users.nom }}</th>
                <th scope="row">{{ users.prenom }}</th>
                <td class="pt-3 d-md-none ">
                    <div class="d-flex justify-content-around">
                        <a href="{{ path('show_user_admin', {'id' : users.id }) }}"><i class="fas fa-search fa-lg"></i></a>
                        <a href="{{ path('edit_user_admin', {'id' : users.id }) }}"><i class="fas fa-edit fa-lg"></i></a>
                        <i class="fas fa-trash-alt fa-lg"></i>
                    </div>
                </td>
                <td class="d-none d-md-block">
                    <div class="btn-group btn-group-toggle" data-toggle="buttons">
                        <label class="btn btn-secondary active">
                            <a type="submit" href="{{ path('show_user_admin', {'id' : users.id }) }}">Voir</a>
                        </label>
                        <label class="btn btn-secondary">
                            <a type="submit" href="{{ path('edit_user_admin', {'id' : users.id }) }}">Modifier</a>
                        </label>
                        <label class="btn btn-danger">
                            <input type="radio" name="options" id="option3" autocomplete="off"> Supprimer
                        </label>
                    </div></td>
            </tr>
            {% endfor %}
            </tbody>
        </table>

    </div>
{% endblock %}
