{% extends 'admin/index.html.twig' %}

{% block body %}
    <div class="col-12 col-md-10 text-center">
        <h1 class="mb-5">{{ title }}</h1>

        <div class="text-right">
        <a href="{{ path('new_user_admin') }}"><button type="submit" class="btn btn-light my-3">Ajouter un participant</button></a>
        </div>

        <table class="table">
            <thead>
            <tr>
                <th class="col-4">Nom</th>
                <th class="col-4">Prénom</th>
                <th class="col-4">Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for users in user %}
            <tr>
                <th scope="row">{{ users.nom }}</th>
                <th scope="row">{{ users.prenom }}</th>
                <td class="pt-3 d-md-none ">
                    <div class="d-flex justify-content-around">
                        <a href="{{ path('show_user_admin', {'id' : users.id }) }}"><i class="fas fa-search fa-lg"></i></a>
                        <a href="{{ path('edit_user_admin', {'id' : users.id }) }}"><i class="fas fa-edit fa-lg"></i></a>
                        <a href="{{ path('delete_user_admin', {'id' : users.id }) }}"><i class="fas fa-delete fa-lg"></i></a>

                    </div>
                </td>
                <td class="d-none d-md-block">
                    <div class="btn-group btn-group-toggle" data-toggle="buttons">
                        <div>
                            <a class="btn btn-secondary" href="{{ path('show_user_admin', {'id' : users.id }) }}">Voir</a>
                        </div>
                        <div>
                            <a class="btn btn-secondary" href="{{ path('edit_user_admin', {'id' : users.id }) }}">Modifier</a>
                        </div>
                        <div>
                            {{ include('admin/user/_delete_form.html.twig') }}
                        </div>
                    </div></td>
            </tr>
            {% endfor %}
            </tbody>
        </table>

    </div>
{% endblock %}
